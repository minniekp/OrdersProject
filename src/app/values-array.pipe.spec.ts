import { ValuesArrayPipe } from './values-array.pipe';

describe('ValuesArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ValuesArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
